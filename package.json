{
  "name": "poof",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:demo": "pnpm --filter demo-track dev",
    "dev:dashboard": "pnpm --filter dashboard dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "dependencies": {
    "esbuild": "^0.15.7"
  }
}
